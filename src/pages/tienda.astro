---
import Layout from "@/layouts/Layout.astro";
import {
  supabase,
  type GiftCard,
  type GiftCardCategory,
  type UserProfile,
} from "@/lib/supabase";
import { DollarSign, Inbox } from "lucide-react";
import StoreNavbar from "@/components/StoreNavbar.astro";

// Verificar autenticación
const {
  data: { session },
} = await supabase.auth.getSession();
let user: UserProfile | null = null;

if (session) {
  const { data } = await supabase
    .from("profiles")
    .select("*")
    .eq("id", session.user.id)
    .single();
  user = data;
}

// Obtener categorías
const { data: categories } = await supabase
  .from("giftcard_categories")
  .select("*")
  .eq("is_active", true)
  .order("name");

// Obtener giftcards con stock disponible (solo las que tienen stock > 0)
const categoryFilter = Astro.url.searchParams.get("categoria");
const sortBy = Astro.url.searchParams.get("orden") || "nombre";
const searchQuery = Astro.url.searchParams.get("buscar") || "";

// Buscar categoría por ID o por nombre
let categoryId = categoryFilter;
if (categoryFilter && categories) {
  // Verificar si es un nombre de categoría en lugar de un ID
  const foundCategory = categories.find(
    (c: any) =>
      c.name.toLowerCase() === categoryFilter.toLowerCase() ||
      c.id === categoryFilter,
  );
  if (foundCategory) {
    categoryId = foundCategory.id;
  }
}

let query = supabase
  .from("giftcards")
  .select(
    `
    *,
    category:giftcard_categories(*)
  `,
  )
  .eq("is_active", true)
  .gt("stock", 0);

if (categoryId) {
  query = query.eq("category_id", categoryId);
}

// Búsqueda por nombre
if (searchQuery) {
  query = query.ilike("name", `%${searchQuery}%`);
}

// Ordenamiento
switch (sortBy) {
  case "precio-asc":
    query = query.order("price", { ascending: true });
    break;
  case "precio-desc":
    query = query.order("price", { ascending: false });
    break;
  case "reciente":
    query = query.order("created_at", { ascending: false });
    break;
  default:
    query = query.order("name");
}

const { data: giftcards } = await query;

// Contar total de productos disponibles
const totalProducts = giftcards?.length || 0;

// Obtener nombre de categoría seleccionada
const selectedCategory = categories?.find((c: any) => c.id === categoryId);
const pageTitle = searchQuery
  ? `Resultados para "${searchQuery}"`
  : selectedCategory
    ? selectedCategory.name
    : "Todos los Productos";
---

<Layout title="Tienda">
  <div
    class="min-h-screen bg-gradient-to-b from-slate-950 via-slate-900 to-slate-950"
  >
    <!-- Header -->
    <header
      class="sticky top-0 z-50 bg-slate-950/95 backdrop-blur-xl border-b border-white/10"
    >
      <!-- Fila superior: Logo, Buscador, Usuario -->
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between gap-4">
          <!-- Logo -->
          <a href="/" class="flex items-center gap-2 shrink-0">
            <div
              class="w-10 h-10 bg-primary rounded-lg flex items-center justify-center"
            >
              <svg
                class="w-6 h-6 text-white"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                ></path>
              </svg>
            </div>
            <span class="text-xl font-bold text-white hidden sm:block"
              >GiftCard Store</span
            >
          </a>

          <!-- Buscador centrado -->
          <div class="flex-1 max-w-2xl">
            <form action="/tienda" method="GET" class="w-full relative">
              <input
                type="text"
                name="buscar"
                placeholder="¿Qué estás buscando?"
                value={Astro.url.searchParams.get("buscar") || ""}
                class="w-full px-4 py-3 pr-12 bg-slate-800 border border-white/10 rounded-lg text-sm text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
              />
              <button
                type="submit"
                class="absolute right-2 top-1/2 -translate-y-1/2 bg-primary hover:bg-primary/90 text-white p-2 rounded-lg transition-all"
              >
                <svg
                  class="w-5 h-5"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
              </button>
            </form>
          </div>

          <!-- Usuario -->
          <div class="flex items-center gap-4 shrink-0">
            {
              user ? (
                <div class="flex items-center gap-4">
                  <div class="hidden sm:block text-right">
                    <p class="text-sm text-white font-medium">
                      {user.full_name}
                    </p>
                    <p class="text-xs text-gray-400 capitalize">{user.role}</p>
                  </div>
                  <div class="relative group">
                    <button class="w-10 h-10 bg-primary/20 rounded-full flex items-center justify-center text-primary font-semibold">
                      {user.full_name?.charAt(0).toUpperCase()}
                    </button>
                    <div class="absolute right-0 top-12 w-48 bg-slate-800 rounded-lg shadow-xl border border-white/10 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all">
                      <div class="p-2">
                        {user.role === "admin" && (
                          <a
                            href="/admin/dashboard"
                            class="block px-4 py-2 text-sm text-gray-300 hover:bg-white/5 rounded-lg"
                          >
                            Ir al Panel
                          </a>
                        )}
                        <a
                          href="/mis-compras"
                          class="block px-4 py-2 text-sm text-gray-300 hover:bg-white/5 rounded-lg"
                        >
                          Mis Compras
                        </a>
                        <a
                          href="/perfil"
                          class="block px-4 py-2 text-sm text-gray-300 hover:bg-white/5 rounded-lg"
                        >
                          Mi Perfil
                        </a>
                        <hr class="my-2 border-white/10" />
                        <a
                          href="/auth/logout"
                          class="block px-4 py-2 text-sm text-red-400 hover:bg-white/5 rounded-lg"
                        >
                          Cerrar Sesión
                        </a>
                      </div>
                    </div>
                  </div>
                </div>
              ) : (
                <div class="flex items-center gap-3">
                  <a
                    href="/auth/login"
                    class="text-gray-400 hover:text-white transition-colors text-sm hidden sm:block"
                  >
                    Iniciar Sesión
                  </a>
                  <a
                    href="/auth/register"
                    class="bg-primary hover:bg-primary/90 text-white px-4 py-2 rounded-lg text-sm font-medium transition-all"
                  >
                    Registrarse
                  </a>
                </div>
              )
            }
          </div>
        </div>
      </div>

      <!-- Fila inferior: Navegación de páginas -->
      <div class="border-t border-white/5 bg-slate-900/50">
        <div class="container mx-auto px-4">
          <nav
            class="flex items-center gap-1 overflow-x-auto py-2 scrollbar-hide"
          >
            <a
              href="/tienda"
              class="flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap bg-primary/20 text-primary transition-all"
            >
              <svg
                class="w-4 h-4"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"
                ></path>
              </svg>
              Tienda
            </a>
            {
              user && (
                <a
                  href="/mis-compras"
                  class="flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap text-gray-400 hover:text-white hover:bg-white/5 transition-all"
                >
                  <svg
                    class="w-4 h-4"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"
                    />
                  </svg>
                  Mis Compras
                </a>
              )
            }
            {
              user?.role === "admin" && (
                <a
                  href="/admin/dashboard"
                  class="flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap text-gray-400 hover:text-white hover:bg-white/5 transition-all"
                >
                  <svg
                    class="w-4 h-4"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"
                    />
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"
                    />
                  </svg>
                  Panel Admin
                </a>
              )
            }
            {
              user?.role === "proveedor" && (
                <a
                  href="/proveedor/dashboard"
                  class="flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap text-gray-400 hover:text-white hover:bg-white/5 transition-all"
                >
                  <svg
                    class="w-4 h-4"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"
                    />
                  </svg>
                  Panel Proveedor
                </a>
              )
            }
          </nav>
        </div>
      </div>
    </header>

    <main class="container mx-auto px-4 py-8">
      <div class="flex flex-col lg:flex-row gap-8">
        <!-- Sidebar - Filtros -->
        <aside class="lg:w-72 shrink-0" id="filters-sidebar">
          <div class="sticky top-24 space-y-6">
            <!-- Categorías -->
            <div
              class="bg-white/5 backdrop-blur-sm rounded-2xl p-6 border border-white/10"
            >
              <div class="flex items-center justify-between mb-4">
                <h3 class="font-semibold text-white">Categoría</h3>
                <button
                  class="text-gray-400 hover:text-white transition-colors"
                  onclick="toggleSection('categories')"
                >
                  <svg
                    class="w-5 h-5 transition-transform"
                    id="categories-icon"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M5 15l7-7 7 7"></path>
                  </svg>
                </button>
              </div>
              <div id="categories-section" class="space-y-1">
                <label
                  class="flex items-center gap-3 p-2 rounded-lg cursor-pointer hover:bg-white/5 transition-all group"
                >
                  <input
                    type="checkbox"
                    checked={!categoryId}
                    onchange="window.location.href='/tienda'"
                    class="w-5 h-5 rounded border-white/20 text-primary focus:ring-primary cursor-pointer"
                  />
                  <span
                    class:list={[
                      "text-sm transition-colors",
                      !categoryId
                        ? "text-white font-medium"
                        : "text-gray-400 group-hover:text-white",
                    ]}>Todos</span
                  >
                </label>
                {
                  categories?.map((cat: any) => (
                    <label class="flex items-center gap-3 p-2 rounded-lg cursor-pointer hover:bg-white/5 transition-all group">
                      <input
                        type="checkbox"
                        checked={categoryId === cat.id}
                        onchange={`window.location.href='/tienda?categoria=${cat.id}'`}
                        class="w-5 h-5 rounded border-white/20 text-primary focus:ring-primary cursor-pointer"
                      />
                      <span
                        class:list={[
                          "text-sm transition-colors",
                          categoryId === cat.id
                            ? "text-white font-medium"
                            : "text-gray-400 group-hover:text-white",
                        ]}
                      >
                        {cat.name}
                      </span>
                    </label>
                  ))
                }
              </div>
            </div>

            <!-- Rango de Precios -->
            <div
              class="bg-white/5 backdrop-blur-sm rounded-2xl p-6 border border-white/10"
            >
              <div class="flex items-center justify-between mb-4">
                <h3 class="font-semibold text-white">Precio</h3>
                <button
                  class="text-gray-400 hover:text-white transition-colors"
                  onclick="toggleSection('price')"
                >
                  <svg
                    class="w-5 h-5 transition-transform"
                    id="price-icon"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M5 15l7-7 7 7"></path>
                  </svg>
                </button>
              </div>
              <div id="price-section" class="space-y-3">
                <div class="flex gap-3">
                  <div class="flex-1">
                    <label class="text-xs text-gray-500 mb-1 block"
                      >Mínimo</label
                    >
                    <input
                      type="number"
                      placeholder="$0"
                      class="w-full px-3 py-2 bg-white/10 border border-white/10 rounded-lg text-sm text-white placeholder-gray-500"
                    />
                  </div>
                  <div class="flex-1">
                    <label class="text-xs text-gray-500 mb-1 block"
                      >Máximo</label
                    >
                    <input
                      type="number"
                      placeholder="$500"
                      class="w-full px-3 py-2 bg-white/10 border border-white/10 rounded-lg text-sm text-white placeholder-gray-500"
                    />
                  </div>
                </div>
              </div>
            </div>
          </div>
        </aside>

        <!-- Contenido Principal -->
        <div class="flex-1">
          <!-- Header de la sección -->
          <div
            class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-8"
          >
            <div>
              <h1 class="text-2xl md:text-3xl font-bold text-white">
                {pageTitle}
                <span class="text-gray-500 font-normal">({totalProducts})</span>
              </h1>
            </div>

            <div class="flex items-center gap-4">
              <!-- Toggle filtros móvil -->
              <button
                id="toggle-filters"
                class="lg:hidden flex items-center gap-2 px-4 py-2 bg-white/10 rounded-lg text-sm text-gray-300 hover:bg-white/20 transition-all"
              >
                <svg
                  class="w-5 h-5"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"
                  ></path>
                </svg>
                Filtros
              </button>

              <!-- Ordenar -->
              <div class="relative">
                <select
                  id="sort-select"
                  class="appearance-none bg-white/10 border-0 rounded-lg px-4 py-2 pr-10 text-sm text-gray-300 cursor-pointer hover:bg-white/20 transition-all focus:ring-2 focus:ring-primary"
                  onchange="handleSort(this.value)"
                >
                  <option value="nombre" selected={sortBy === "nombre"}
                    >Ordenar por nombre</option
                  >
                  <option value="precio-asc" selected={sortBy === "precio-asc"}
                    >Precio: menor a mayor</option
                  >
                  <option
                    value="precio-desc"
                    selected={sortBy === "precio-desc"}
                    >Precio: mayor a menor</option
                  >
                  <option value="reciente" selected={sortBy === "reciente"}
                    >Más recientes</option
                  >
                </select>
                <svg
                  class="absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400 pointer-events-none"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M19 9l-7 7-7-7"></path>
                </svg>
              </div>
            </div>
          </div>

          {/* Grid de GiftCards */}
          {
            giftcards && giftcards.length > 0 ? (
              <div class="grid sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4 gap-6">
                {giftcards.map((card, index) => (
                  <a
                    href={`/giftcard/${card.id}`}
                    class="product-card group bg-white/5 backdrop-blur-sm rounded-2xl overflow-hidden border border-white/10 hover:border-primary/50 transition-all duration-300 hover:shadow-xl hover:shadow-primary/10"
                    style={`animation-delay: ${index * 50}ms`}
                  >
                    {/* Imagen del producto */}
                    <div class="relative aspect-square bg-gradient-to-br from-slate-800 to-slate-900 overflow-hidden">
                      {card.image_url ? (
                        <img
                          src={card.image_url}
                          alt={card.name}
                          class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                        />
                      ) : (
                        <div class="w-full h-full flex items-center justify-center">
                          <div class="text-6xl font-bold text-slate-700 group-hover:scale-110 transition-transform duration-500">
                            {card.name?.charAt(0)}
                          </div>
                        </div>
                      )}

                      {/* Badge de descuento */}
                      {card.original_price &&
                        card.original_price > card.price && (
                          <span class="absolute top-3 left-3 bg-green-500 text-white text-xs font-bold px-2.5 py-1 rounded-full shadow-lg">
                            -
                            {Math.round(
                              (1 - card.price / card.original_price) * 100,
                            )}
                            %
                          </span>
                        )}

                      {/* Badge de stock */}
                      <span
                        class:list={[
                          "absolute top-3 right-3 text-xs font-semibold px-2.5 py-1 rounded-full shadow-lg flex items-center gap-1.5",
                          card.stock > 10
                            ? "bg-green-500 text-white"
                            : "bg-amber-500 text-white",
                        ]}
                      >
                        <span class="w-1.5 h-1.5 rounded-full bg-current opacity-75 animate-pulse" />
                        {card.stock > 10
                          ? "Disponible"
                          : `${card.stock} disponibles`}
                      </span>

                      {/* Overlay hover */}
                      <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-6">
                        <span class="bg-white text-slate-900 px-6 py-2 rounded-full text-sm font-semibold transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">
                          Ver detalles
                        </span>
                      </div>
                    </div>

                    {/* Info del producto */}
                    <div class="p-5">
                      {/* Categoría */}
                      <span class="inline-block text-xs font-medium text-gray-400 bg-white/10 px-2.5 py-1 rounded-full mb-3">
                        {card.category?.name}
                      </span>

                      {/* Nombre */}
                      <h3 class="font-semibold text-white group-hover:text-primary transition-colors mb-2 line-clamp-1">
                        {card.name}
                      </h3>

                      {/* Precios */}
                      <div class="flex items-baseline gap-2">
                        <span class="text-xl font-bold text-white">
                          ${card.price.toFixed(2)}
                        </span>
                        {card.original_price &&
                          card.original_price > card.price && (
                            <span class="text-sm text-gray-500 line-through">
                              ${card.original_price.toFixed(2)}
                            </span>
                          )}
                      </div>
                    </div>
                  </a>
                ))}
              </div>
            ) : (
              /* Estado vacío profesional */
              <div class="flex flex-col items-center justify-center py-20 px-4">
                <div class="relative mb-8">
                  <div class="w-32 h-32 bg-gradient-to-br from-slate-800 to-slate-900 rounded-full flex items-center justify-center">
                    <svg
                      class="w-16 h-16 text-slate-600"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="1.5"
                        d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"
                      />
                    </svg>
                  </div>
                  <div class="absolute -bottom-2 -right-2 w-12 h-12 bg-primary/10 rounded-full flex items-center justify-center">
                    <svg
                      class="w-6 h-6 text-primary"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"
                      />
                    </svg>
                  </div>
                </div>

                <h3 class="text-2xl font-bold text-white mb-3 text-center">
                  Lo sentimos, no hay stock disponible
                </h3>
                <p class="text-gray-400 text-center max-w-md mb-8 leading-relaxed">
                  En este momento no contamos con productos disponibles
                  {selectedCategory &&
                    ` en la categoría "${selectedCategory.name}"`}
                  . Estamos trabajando para reponer nuestro inventario muy
                  pronto.
                </p>

                <div class="flex flex-col sm:flex-row gap-4">
                  {categoryFilter && (
                    <a
                      href="/tienda"
                      class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-primary hover:bg-primary/90 text-white rounded-xl font-medium transition-all hover:scale-105 shadow-lg shadow-primary/25"
                    >
                      <svg
                        class="w-5 h-5"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M4 6h16M4 10h16M4 14h16M4 18h16"
                        />
                      </svg>
                      Ver todas las categorías
                    </a>
                  )}
                  <a
                    href="/"
                    class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 text-white rounded-xl font-medium transition-all hover:scale-105"
                  >
                    <svg
                      class="w-5 h-5"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
                      />
                    </svg>
                    Volver al inicio
                  </a>
                </div>

                <div class="mt-12 p-6 bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 max-w-md">
                  <div class="flex items-start gap-4">
                    <div class="w-10 h-10 bg-primary/10 rounded-full flex items-center justify-center shrink-0">
                      <svg
                        class="w-5 h-5 text-primary"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"
                        />
                      </svg>
                    </div>
                    <div>
                      <h4 class="font-semibold text-white mb-1">
                        ¿Quieres que te avisemos?
                      </h4>
                      <p class="text-sm text-gray-400">
                        Suscríbete y te notificaremos cuando tengamos nuevos
                        productos disponibles.
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            )
          }
        </div>
      </div>
    </main>
  </div>
</Layout>

<style>
  /* Animaciones de entrada */
  .product-card {
    animation: fadeInUp 0.5s ease-out forwards;
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Animación suave para hover */
  .product-card:hover {
    transform: translateY(-4px);
  }

  /* Mejora de transición para imágenes */
  .product-card img {
    will-change: transform;
  }

  /* Línea de truncado */
  .line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Ocultar scrollbar en nav */
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }

  /* Mobile filters */
  @media (max-width: 1023px) {
    #filters-sidebar {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: 100%;
      max-width: 320px;
      background: rgb(15 23 42);
      z-index: 100;
      transform: translateX(-100%);
      transition: transform 0.3s ease;
      overflow-y: auto;
      padding: 1.5rem;
    }

    #filters-sidebar.open {
      transform: translateX(0);
    }
  }

  /* Overlay para móvil */
  .filters-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 99;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
  }

  .filters-overlay.open {
    opacity: 1;
    visibility: visible;
  }
</style>

<script>
  // Toggle filtros móvil
  const toggleFilters = document.getElementById("toggle-filters");
  const sidebar = document.getElementById("filters-sidebar");

  if (toggleFilters && sidebar) {
    // Crear overlay
    const overlay = document.createElement("div");
    overlay.className = "filters-overlay";
    document.body.appendChild(overlay);

    toggleFilters.addEventListener("click", () => {
      sidebar.classList.toggle("open");
      overlay.classList.toggle("open");
    });

    overlay.addEventListener("click", () => {
      sidebar.classList.remove("open");
      overlay.classList.remove("open");
    });
  }

  // Función para manejar ordenamiento
  function handleSort(value: string) {
    const url = new URL(window.location.href);
    url.searchParams.set("orden", value);
    window.location.href = url.toString();
  }

  // Función para toggle de secciones
  function toggleSection(section: string) {
    const content = document.getElementById(`${section}-section`);
    const icon = document.getElementById(`${section}-icon`);

    if (content && icon) {
      content.classList.toggle("hidden");
      icon.style.transform = content.classList.contains("hidden")
        ? "rotate(180deg)"
        : "";
    }
  }

  // Exponer funciones al scope global
  (window as any).handleSort = handleSort;
  (window as any).toggleSection = toggleSection;
</script>
